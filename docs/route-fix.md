# اصلاحات مسیریابی – 2025-12-02

این اصلاح‌ها روی `routes/web.php` انجام شد:

- حذف مسیرهای تکراری کلاس‌های دانش‌آموز (`/dashboard/student/classrooms` و `.../{classroom}`) و نگه داشتن یک‌بار تعریف هرکدام زیر کنترلر `StudentClassController` برای جلوگیری از هم‌پوشانی یا override بی‌صدا.
- حذف `Route::view('/profile', ...)` و نگه داشتن تنها مسیر کنترلری پروفایل دانش‌آموز (`StudentProfileController`) تا نام مسیر `student.profile` به‌صورت شفاف به اکشن ویرایش متصل باشد.
- بازآرایی و فرمت مجدد فایل برای خوانایی بهتر (تورفتگی‌ها و فاصله‌گذاری‌ها).

هدف: کاهش تعارض در تعریف route ها و شفاف کردن نگاشت نام مسیرها به اکشن‌ها.
